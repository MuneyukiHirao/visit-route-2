{
  "name": "visit-route-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "set PYTHONPATH=src&& python -m pytest",
    "test:frontend": "cd frontend && npm test"
  }
}
